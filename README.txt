https://github.com/itsabigaundy/ml_fa21

Clone the above github repository, create a virtual environment with Python 3.8.5.

Run 'pip install -r requirements.txt' in the root directory.

Run unsupervised.py